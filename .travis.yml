language: python

os:
  - linux

addons:
  apt:
    packages:
      - graphviz

python:
  - 2.7

notifications:
  email: false

install:
  - pip install pytest pytest-cov coveralls
  - python setup.py install

script:
  - py.test --cov=phylosfs --cov-report term-missing
